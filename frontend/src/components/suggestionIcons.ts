import { BookIcon, CodeIcon, PenIcon, SparkIcon } from './Icons'

/** Kept out of Icons.tsx so that file only exports components (fast refresh). */
export const SUGGESTION_ICONS = {
  code: CodeIcon,
  spark: SparkIcon,
  book: BookIcon,
  pen: PenIcon,
}
